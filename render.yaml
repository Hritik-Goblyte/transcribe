services:
  - type: web
    name: transcribe-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    runtime: python-3.10
    envVars:
      - key: BOT_TOKEN
        sync: false
